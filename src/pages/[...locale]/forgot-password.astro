---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Navbar from "../../components/Navbar.astro";
import Footer from "../../components/Footer.astro";
import ForgotPasswordForm from "../../components/ForgotPasswordForm.astro";
import { getLocaleFromUrl, loadTranslations, getLangPrefix } from "../../i18n";

export const prerender = false;

const locale = getLocaleFromUrl(Astro.url);
const translations = await loadTranslations(locale);
const langPrefix = getLangPrefix(locale);
---

<BaseLayout title={`${translations["auth.forgotPassword.title"]} - ${translations["site.title"]}`} lang={locale}>
  <Navbar currentLocale={locale} translations={translations} getLangPrefix={getLangPrefix} />

  <main class="container section">
    <ForgotPasswordForm translations={translations} loginUrl={`${langPrefix}login`} />
  </main>

  <Footer translations={translations} currentLocale={locale} getLangPrefix={getLangPrefix} />
</BaseLayout>

<style>
  /* Layout wrapper styles for forgot-password page */
</style>

---
interface Props {
  translations: Record<string, string>;
}

const { translations } = Astro.props;
---

<section class="stats-grid grid-auto">
  <article class="stat-card card hover-lift">
    <div class="stat-header">
      <span class="stat-icon">👥</span>
      <span class="stat-trend positive">—</span>
    </div>
    <div class="stat-value">—</div>
    <div class="stat-label">{translations["nav.users"]}</div>
  </article>

  <article class="stat-card card hover-lift">
    <div class="stat-header">
      <span class="stat-icon">📋</span>
      <span class="stat-trend positive">—</span>
    </div>
    <div class="stat-value">—</div>
    <div class="stat-label">{translations["nav.analytics"]}</div>
  </article>

  <article class="stat-card card hover-lift">
    <div class="stat-header">
      <span class="stat-icon">📈</span>
      <span class="stat-trend positive">—</span>
    </div>
    <div class="stat-value">—</div>
    <div class="stat-label">{translations["nav.settings"]}</div>
  </article>

  <article class="stat-card card hover-lift">
    <div class="stat-header">
      <span class="stat-icon">⚙️</span>
    </div>
    <div class="stat-value">—</div>
    <div class="stat-label">{translations["nav.settings"]}</div>
  </article>
</section>

<div class="dashboard-main-grid">
  <div class="main-col">
    <article class="card card-glass">
      <div class="card-title-row">
        <h3>{translations["nav.users"]}</h3>
      </div>
      <p class="text-muted">Panel de administración activo</p>
    </article>
  </div>

  <div class="side-col">
    <article class="card">
      <h3 class="card-section-title">{translations["nav.settings"]}</h3>
      <ul class="activity-feed">
        <li class="activity-item">
          <span class="activity-icon">🔐</span>
          <div class="activity-details">
            <span class="activity-title">Firebase Auth</span>
            <span class="activity-time">Configurado</span>
          </div>
          <span class="badge badge-accent">Activo</span>
        </li>
        <li class="activity-item">
          <span class="activity-icon">🌍</span>
          <div class="activity-details">
            <span class="activity-title">i18n</span>
            <span class="activity-time">ES · EN · CA</span>
          </div>
          <span class="badge badge-accent">Activo</span>
        </li>
      </ul>
    </article>
  </div>
</div>

<style>
  .stats-grid {
    margin-bottom: var(--space-xl);
  }

  .stat-card {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .stat-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .stat-icon {
    font-size: 1.5rem;
  }

  .stat-trend {
    font-size: var(--font-size-xs);
    font-weight: 700;
  }

  .stat-trend.positive {
    color: var(--color-success);
  }

  .stat-value {
    font-size: var(--font-size-3xl);
    font-weight: 800;
    color: var(--color-text);
  }

  .stat-label {
    font-size: var(--font-size-xs);
    color: var(--color-text-muted);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .dashboard-main-grid {
    display: grid;
    grid-template-columns: 1.6fr 1fr;
    gap: var(--space-lg);
  }

  .card-title-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--space-lg);
  }

  .card-section-title {
    font-size: var(--font-size-lg);
    margin-bottom: var(--space-md);
  }

  .text-muted {
    color: var(--color-text-muted);
    font-size: var(--font-size-xs);
  }

  .activity-feed {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .activity-item {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding-bottom: var(--space-sm);
    border-bottom: 1px solid var(--color-surface-border);
  }

  .activity-item:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .activity-icon {
    font-size: 1.4rem;
    padding: var(--space-xs);
    background-color: var(--color-surface-hover);
    border-radius: var(--border-radius-sm);
  }

  .activity-details {
    display: flex;
    flex-direction: column;
    flex: 1;
  }

  .activity-title {
    font-size: var(--font-size-sm);
    font-weight: 600;
    color: var(--color-text);
  }

  .activity-time {
    font-size: var(--font-size-xs);
    color: var(--color-text-muted);
  }

  @media (max-width: 900px) {
    .dashboard-main-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .stat-value {
      font-size: var(--font-size-2xl);
    }
  }
</style>
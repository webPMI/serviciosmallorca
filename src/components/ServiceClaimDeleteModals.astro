---
import { type Locale } from "../../../i18n";

export const ServiceClaimDeleteModals = ({
  service,
  translations,
  locale
}: {
  service: any;
  translations: any;
  locale: Locale;
}) => {
  return (
    <section class="card section-box claim-delete-section">
      <h2 class="box-title">{translations["services.claims"]}</h2>
      <div class="claims-links">
        <a href="#" class="btn btn-outline">
          <span>{translations["services.claim_property"]}</span>
        </a>
        <a href="#" class="btn btn-outline">
          <span>{translations["services.request_deletion"]}</span>
        </a>
      </div>
    </section>
  );
};
